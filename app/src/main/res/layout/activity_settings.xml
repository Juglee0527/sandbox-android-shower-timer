<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/tvTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Settings"
        android:textSize="24sp"
        android:textStyle="bold"
        android:layout_marginTop="24dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- 총 샤워 시간 -->
    <EditText
        android:id="@+id/etTotalMinutes"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:hint="총 샤워 시간(분)"
        android:inputType="number"
        android:layout_marginTop="32dp"
        app:layout_constraintTop_toBottomOf="@id/tvTitle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginStart="32dp"
        android:layout_marginEnd="32dp"/>

    <!-- 알림 간격 -->
    <EditText
        android:id="@+id/etintervalSeconds"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:hint="알림 간격(초)"
        android:inputType="number"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@id/etTotalMinutes"
        app:layout_constraintStart_toStartOf="@id/etTotalMinutes"
        app:layout_constraintEnd_toEndOf="@id/etTotalMinutes"/>

    <!-- 유튜브 링크/검색어 -->
    <EditText
        android:id="@+id/etYoutube"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:hint="YouTube 링크 또는 검색어"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@id/etintervalSeconds"
        app:layout_constraintStart_toStartOf="@id/etTotalMinutes"
        app:layout_constraintEnd_toEndOf="@id/etTotalMinutes"/>

    <!-- 음성 안내 On/Off -->
    <Switch
        android:id="@+id/switchVoice"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="음성 안내 사용"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@id/etYoutube"
        app:layout_constraintStart_toStartOf="@id/etTotalMinutes" />

    <!-- 저장 버튼 -->
    <Button
        android:id="@+id/btnSave"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="저장"
        android:layout_marginTop="32dp"
        app:layout_constraintTop_toBottomOf="@id/switchVoice"
        app:layout_constraintStart_toStartOf="@id/etTotalMinutes"
        app:layout_constraintEnd_toEndOf="@id/etTotalMinutes"/>

</androidx.constraintlayout.widget.ConstraintLayout>
